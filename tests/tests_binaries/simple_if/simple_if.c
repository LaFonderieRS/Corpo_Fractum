// Header file for input output functions
#include <stdio.h>

// Main function: entry point for execution
int main() {
    // This condition is always true, so the decompilation should not display it, just the printf()
    if (20 > 18) {
        printf("20 is greater than 18\n");
    }
}
