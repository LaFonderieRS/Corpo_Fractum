// Header file for input output functions
#include <stdio.h>

// Main function: entry point for execution
int main(int argc, char *argv[]) {
    // Just retrieve the argc
    if (20 > 18) {
        printf("%i arguments\n", argc);
    }
}
